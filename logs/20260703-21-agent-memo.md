# 2026-07-03 21:00 Agent Memo

## Session Context

I started from the worker prompt naming
`/Users/test/git/busdk/agent-supervisor/projects/busdk/.bus/services/workers/runtime/3fe3ce83-95e8-415a-824f-e2eb72b76439/qemu-worktree`
as the authoritative checkout on branch
`worker/qemu-rv64-ld32u-env-20260703a`, base `a20a292b4b`. The initially
provided shell directory was the sibling `product-worktree/qemu`, which was
clean and on the same branch name but had a different HEAD. I treated the
prompt path as authoritative after verifying that `qemu-worktree` was clean,
on the requested branch, and exactly at
`a20a292b4b8b19fef901beff657a9fc81ec96c7b`.

The QEMU-local `AGENTS.md` says to keep this generic to upstream QEMU, read
`PLAN.md`, use deterministic tests for emitter/parser questions, and reserve
browser runs for gate decisions. `PLAN.md` shows the current RISC-V generated
execution lane is still open around R4d. The requested work is a deterministic
implementation slice: lower bounded env-relative `ld32u` and related memory
forms according to the documented ABI, extend generated-output equivalence
fixtures, run deterministic tests only, and commit without pushing.

The expected effect is to increase the tested generated-output shape coverage
for env-relative memory accesses without making a browser speed claim. The
mechanism should be a fail-closed emitter path that accepts only bounded
offsets from the run context `env` pointer and preserves the existing helper,
SoftMMU, and unsupported fallback boundaries for other memory shapes.

## Work Narrative

I found no `GOAL.md` inside the QEMU checkout, and a shallow search of the
runtime tree did not locate a supervisor-root copy. I continued with the local
QEMU guidance and `PLAN.md`, which are enough for this scoped deterministic
slice.

I inspected `scripts/ci/wasm-generated-output-equivalence-test.mjs` and found
that the shared per-TB deterministic emitter already emitted raw direct-memory
`ld32u`, `ld32s`, `ld`, `st8`, `st32`, and `st` operations. The risk was that
the model accepted any register-derived host address. I narrowed that path so
direct-memory operations lower only when their base is the modeled env anchor
register and the signed 16-bit offset plus access width stays within the
bounded fixture ABI window. I then added one RV64 env-relative fixture that
exercises the load/store family without helper calls, plus fail-closed
fixtures for a non-env base register and for an out-of-range store. This keeps
the work as deterministic coverage over the ABI shape; it does not enable a
browser run or claim real guest speedup.

After the first equivalence check passed, I checked the translation metadata
contract and found that `tcg/wasm64.c` still blocked direct host-memory opcodes
before generated-output words were captured. That would have made the new
operand-level bounds unreachable for live generated-output coverage. I changed
the generated-support predicate to record direct `ld`, `ld32s`, `ld32u`, `st`,
`st8`, and `st32` words again, with a comment making the safety split explicit:
metadata capture is opcode-level, while the generated-output emitter is
responsible for bounded env-relative lowering and fail-closed rejection. I
also added the same env-relative bound check to the embedded live one-TB
JavaScript emitter in `tcg/wasm64.c` so unsafe direct-memory operands do not
produce a generated body.

The deterministic checks passed after those changes:
`node --check scripts/ci/wasm-generated-output-equivalence-test.mjs`,
`node scripts/ci/wasm-generated-output-equivalence-test.mjs`,
`node --check scripts/ci/wasm64-translate-metadata-test.mjs`,
`node scripts/ci/wasm64-translate-metadata-test.mjs`, and scoped
`git diff --check` over the touched implementation and test files. The
equivalence run reported `fixtures=19`, `unsupportedFixtures=3`, and
`rv64EnvRelativeFixtures.fixtureExecutions=2` with zero env-relative helper
calls. I recorded the slice in `PLAN.md` as R4d-d and kept R4d open for the
future browser proof.

## Final State

The bounded env-relative direct-memory slice is implemented in the QEMU
checkout. The touched implementation files are `tcg/wasm64.c`,
`scripts/ci/wasm-generated-output-equivalence-test.mjs`,
`scripts/ci/wasm64-translate-metadata-test.mjs`, and `PLAN.md`, with this memo
added under `logs/`. The deterministic checks completed successfully:

- `node --check scripts/ci/wasm-generated-output-equivalence-test.mjs`
- `node scripts/ci/wasm-generated-output-equivalence-test.mjs`
- `node --check scripts/ci/wasm64-translate-metadata-test.mjs`
- `node scripts/ci/wasm64-translate-metadata-test.mjs`
- `git diff --check`

No browser run, artifact build, push, BusDK submodule update, or Bus Engine OS
proof was run. The remaining next step for the broader R4d lane is still a
supervisor-run Chrome proof that real RV64 generated coverage is nonzero and
that remaining fallback attribution is captured before another R4c speed gate.
