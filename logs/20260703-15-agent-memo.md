## Session Context

This session started as the QEMU worker for task-45483bf1047a, the R4d lane to
prove real RV64 guest generated coverage. The authoritative checkout is the
manual QEMU worktree at
`/Users/test/git/busdk/agent-supervisor/projects/busdk/.bus/services/workers/runtime/460472e7-9a29-41b5-b9fe-07582594e84a/qemu-worktree`,
not the similarly named BusDK product-worktree. The branch is
`worker/qemu-riscv64-r4d-generated-coverage-20260703b` at `f438e41a9c`, which
the task states is the current authoritative base on `develop`.

I read the local QEMU `AGENTS.md` and the R4 section of `PLAN.md` before
changing files. The plan says R4d remains open until the browser result JSON
shows nonzero generated execution coverage from actual guest TBs before
`Welcome to TuxTest`, records top fallback PCs/TBs or unsupported op shapes,
and has deterministic coverage proving strict TCI fallback remains the
default. I could not find the named supervisor brief at
`logs/worker-briefs/20260703-qemu-riscv64-r4d-generated-coverage.md` in or
near this checkout; the direct expected supervisor path was absent from this
sandbox view and a broader search outside the worktree timed out. I am using
the explicit task text and checked-in plan as the working brief.

## Work Narrative

The first inspection confirmed that the worktree is clean on the requested
branch and that the active code surface is the existing wasm64 TCG path:
`tcg/wasm64.c`, `tcg/wasm64.h`, `tcg/wasm64/tcg-target.c.inc`, and the
deterministic Node tests under `scripts/ci`. A broad `rg` showed existing
coverage, fallback, hot-block, and metadata tests, especially
`scripts/ci/wasm64-translate-metadata-test.mjs`, which already checks that
live translation metadata is captured and that generated/fallback counters are
reported. The next step is to inspect those exact contracts and patch the
smallest lowering/emitter slice that can turn a real RV64 guest TB from a
metadata-only candidate into a generated execution while preserving strict TCI
as the default.

The sandbox initially denied creating `logs/` because the manual worktree is
outside the default writable roots. I requested and received an escalated write
for the memo directory. This is an environment constraint rather than a source
tree signal; all product changes must still stay in this manual worktree and
on the worker branch.

## Hourly Handoff

The first hour established the authoritative branch and narrowed the R4d work
to the wasm64 TCG runtime and browser-runner contract. The key finding was that
the existing generated metric path was an x86-specific live one-TB differential
and could not prove RV64 guest generated coverage. I started a new opt-in
`QEMU_WASM64_LIVE_TB_COVERAGE` path intended to scan real live TB metadata,
compile a generated WebAssembly body from a conservative side-effect-free TCI
word subset, compare it against a JavaScript reference over the same live TCI
words, and then let strict TCI remain the guest-state commit path. The work
continued into the next hour with code already patched but not yet committed.
